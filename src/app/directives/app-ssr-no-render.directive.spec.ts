import { AppSsrNoRenderDirective } from './app-ssr-no-render.directive';

describe('AppSsrNoRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSsrNoRenderDirective();
    expect(directive).toBeTruthy();
  });
});
