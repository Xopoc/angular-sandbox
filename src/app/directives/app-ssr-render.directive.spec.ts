import { AppSsrRenderDirective } from './app-ssr-render.directive';

describe('AppSsrRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSsrRenderDirective();
    expect(directive).toBeTruthy();
  });
});
